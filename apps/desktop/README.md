# Aplicación de escritorio

Visor local basado en Tkinter y biblioteca estándar:

```bash
python apps/desktop/main.py
```

Lee el mismo catálogo que la PWA. El workflow `desktop.yml` muestra cómo
empaquetarlo con PyInstaller; el ZIP fuente no afirma incluir un binario firmado.
